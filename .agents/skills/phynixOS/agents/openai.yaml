interface:
  display_name: "PhynixOS"
  short_description: "Repo-specific patterns and workflows for phynixOS"
  default_prompt: "Use the phynixOS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true